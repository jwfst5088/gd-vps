RewriteEngine On
RewriteBase /

# API proxy via PHP router
RewriteRule ^(api/.*|ping)$ router.php [QSA,L]

# SPA fallback: serve index.html for non-file requests
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
